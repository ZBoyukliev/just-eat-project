import styles from './Header.module.css'

const Header = () => {
    return (
        <header className='header'>
            <section>
            </section>

            <nav >
            </nav>
        </header>
    );
};

export default Header;